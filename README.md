# pr-archive

A tool to archive GitHub pull requests in a GitHub repository.

See .config for a sample config.

Example usage:

## Fetch PRs info:

  $ ./fetch.sh /tmp/prs

## Archive everything:

  $ ./pull.sh /tmp/prs --all

